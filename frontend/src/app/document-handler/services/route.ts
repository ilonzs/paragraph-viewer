/**
 * Route enum for REST requests
 */
export enum Route {
  DocumentServerUrl = 'http://localhost:8080',
  DocumentApi = '/document-api',
  GetDocumentLength = '/document/length',
  GetDocument = '/document'
}
